#include<stdio.h>
int gcd(int a,int b);
main()
{
	int a,b;
	printf("Ente a: & b:");
	scanf("%d%d",&a,&b);
}
int gcd(int a,int b)
{
	int i;
	for(i=0;i<=a&&i<=b;i++)
	{
		if(a%i==0 && b%i==0)
		{
			
		}
	}
	
}